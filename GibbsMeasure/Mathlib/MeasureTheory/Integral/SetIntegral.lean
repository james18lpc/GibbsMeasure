/-!
# TODO

* Rename `integral_univ` to `setIntegral_univ`
* Make `integral_indicator`/`lintegral_indicator` have explicit arguments
  `(hs : MeasurableSet s) (f : ...)` in that order
* Change the RHS of `setLIntegral_const` to `μ s * c`
-/
